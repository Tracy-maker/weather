import { Box } from '@mui/material';
import styled from 'styled-components';

const ForeCastContainer= styled(Box)`
text-align:center;
`

function Weeks() {
  return <ForeCastContainer>Week</ForeCastContainer>;
}
export default Weeks;
